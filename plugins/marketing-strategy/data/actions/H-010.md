---
id: H-010
logged: 2026-04-15
logged_by: daily-marketing-brief (seed)
channel: ai-agent
status: SEEDED
window_end: 2026-04-29
hypothesis: ai_citation_rate_7d becomes non-null with ≥1 citation on ≥1 canonical query within 14d after deploying the rewritten llms.txt + running weekly probes
metric: ai_citation_rate_7d
baseline: null
target_delta: first non-null
target_url: https://systemprompt.io
posted_at: null
live_url: null
---

# H-010 — Deploy llms.txt rewrite + begin AEO probe loop

**One-line rationale:** The llms.txt rewrite is committed (web-repo `414f88d`) but live systemprompt.io still serves the generic pre-rewrite version. The deploy unlocks every downstream AEO hypothesis. Parallel to that, we must start actually measuring AI citation rate so we know whether the rewrite is working.

## Part 1 — Deploy the llms.txt rewrite

**Critical: switch profile before running jobs.** Per memory rule, jobs run against `local`:

```bash
systemprompt admin session switch local

# Rebuild the binary with the new sections.rs
just build

# Regenerate llms.txt from the updated Rust code
systemprompt infra jobs run llms_txt_generation

# Deploy to production (Ed's normal deploy flow)
# (deploy command depends on current deployment process)

# After deploy, switch back to prod for verification
systemprompt admin session switch systemprompt-prod
```

**Verification:**

```bash
curl -s https://systemprompt.io/llms.txt | head -40
```

Expected: the intro paragraph should start with "AI governance infrastructure for organisations running agents at scale" (not "Production infrastructure for AI agents"). If it still shows the old text, the job ran on the wrong profile or the deploy didn't propagate.

## Part 2 — Probe setup

Before citing any `ai_citation_rate_7d` values, API keys for at least one of these probe targets must be provisioned:

| Target | Auth env var | Approx cost/week |
|---|---|---|
| Perplexity API (`sonar` or `sonar-pro`) | `PERPLEXITY_API_KEY` | <$1 |
| Anthropic Messages API with `web_search_20250305` | `ANTHROPIC_API_KEY` | ~$2 |
| OpenAI Responses API with `web_search` | `OPENAI_API_KEY` | ~$2 |
| Google Gemini with grounding | `GEMINI_API_KEY` | <$1 |

Budget request: **<$10/week total** to run 15 canonical queries against all 4 targets weekly. That's the entire AEO measurement loop.

**Manual fallback:** if no API keys are provisioned this week, Ed runs the probes manually: paste each of the 15 canonical queries into claude.ai / chatgpt.com / perplexity.ai / gemini.google.com, classify each result, record in `data/probe-results.jsonl`. Estimated 45 minutes for 15 queries × 4 targets.

## Part 3 — First probe run

Once Part 1 and Part 2 are both done, run a probe baseline. Store in:

```
/var/www/html/systemprompt-marketplace/plugins/marketing-strategy/data/probe-results.jsonl
```

One JSON line per `(date, query, target, classification, snippet_of_answer, sources_cited)`. Classifications: `CITED_FIRST`, `CITED_SECONDARY`, `NOT_CITED_BUT_COMPETITORS`, `NOT_CITED_NO_COMPETITORS`.

## Hypothesis scoring on 2026-04-29

At `window_end` (2026-04-29), run the probe a second time. Compare to baseline. The hypothesis PASSES if:
- At least 1 canonical query produces a `CITED_FIRST` or `CITED_SECONDARY` result on at least 1 probe target, AND
- `ai_citation_rate_7d` is non-null in `lead-tracker`'s metric tail

FAIL if zero citations across all queries and all targets after 14 days. Lesson if FAIL: the llms.txt rewrite alone is insufficient for AEO lift; we need comparison pages + awesome-list entries before citation rates move.

## After completing each part

Reply:
- `done H-010 part1 {deploy URL}` after llms.txt goes live
- `done H-010 part2 {API keys provisioned list or "manual"}` after probe setup
- `done H-010 part3 {probe result file link}` after baseline probe

Check-back 2026-04-29.
